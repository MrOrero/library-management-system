import { AbstractRepo } from "../../../libs/db/AbstractRepo";
import { BorrowedRecords } from "../models/borrowed-record.model";

export class BorrowedRecordRepository extends AbstractRepo<BorrowedRecords> {
}
